apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
  annotations:
    avp.kubernetes.io/path: "k8secrets/metallb"  
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      - <addresspool>