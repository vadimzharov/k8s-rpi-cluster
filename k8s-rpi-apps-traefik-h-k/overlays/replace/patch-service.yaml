apiVersion: v1
kind: Service
metadata:
  name: traefik
  annotations:
    avp.kubernetes.io/path: "k8secrets/traefik"    
spec:
  type: LoadBalancer
  loadBalancerIP: <traefikingressip>
  ports:
  - port: 6443
    name: websecure-ext
    targetPort: "websecure-ext"
    protocol: "TCP"   