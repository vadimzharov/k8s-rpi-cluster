apiVersion: v1
kind: Service
metadata:
  name: mikrotik-exporter
  annotations:
    service.cilium.io/global: "true"    
  labels:
    app.kubernetes.io/name: mikrotik-exporter
spec:
  ports:
    - name: metrics
      protocol: TCP
      port: 9436
      targetPort: 9436
  selector:
    app.kubernetes.io/name: mikrotik-exporter
  type: ClusterIP

