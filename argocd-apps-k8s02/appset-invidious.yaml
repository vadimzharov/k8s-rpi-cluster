apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: invidious
  namespace: argocd
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          shared-apps: "true"
  template:
    metadata:
      name: '{{name}}-invidious' 
    spec:
      project: k8s-rpi-apps
      sources:
      - repoURL: https://github.com/vadimzharov/k8s-rpi-cluster.git
        targetRevision: k8s-second
        path: k8s-rpi-apps-invidious/main
        plugin:
      - repoURL: https://github.com/vadimzharov/k8s-rpi-cluster.git
        targetRevision: k8s-second
        path: k8s-rpi-apps-invidious/novpn
        plugin:        
      destination:
        server: '{{server}}'
        namespace: invidious
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          