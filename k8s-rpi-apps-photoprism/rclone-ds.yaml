apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: rclone-s3-photos-mount
  labels:
    app: rclone-photos
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: rclone-photos
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rclone-photos
    spec:
      terminationGracePeriodSeconds: 120
      containers:
      - name: rclone-s3-photos-mount
        image: "docker.io/rclone/rclone:latest"
        args:
          - mount
          - "minio:photos"
          - "/mnt/s3-photos"
          - --config=/etc/rclone/rclone.conf
          - --cache-info-age=4h
          - --cache-chunk-clean-interval=15m
          - --dir-cache-time=5s
          - --vfs-cache-mode=writes
          - --allow-non-empty=true
          - --allow-other=true
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh","-c","fusermount -uz /mnt/s3-photos"]
        securityContext:
          privileged: true
          capabilities:
            add:
              - SYS_ADMIN
        resources:
        volumeMounts:
          - name: config-data
            subPath: config.conf
            mountPath: /etc/rclone/rclone.conf
          - name: data
            mountPath: /var/lib/rclone
          - name: media
            mountPath: /mnt/s3-photos
            mountPropagation: Bidirectional
      volumes:
      - name: config-data
        secret:
          secretName: rclone-s3-config
      - name: media
        hostPath:
          path: /mnt/s3-photos
      - name: data
        hostPath:
          path: /var/lib/rclone-photos/
      nodeSelector:
        shared-s3: "true"
