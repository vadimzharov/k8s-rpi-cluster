apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoprism-old-photos
spec:
  storageClassName: nfs-csi      
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: photoprism-old-photos
      