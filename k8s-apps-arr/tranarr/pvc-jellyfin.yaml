apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tranarr-dedicated-movies
spec:
  storageClassName: nfs-csi      
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: tranarr-dedicated-movies
      