apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-m-photos
spec:
  storageClassName: nfs-csi      
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: immich-m-photos
      