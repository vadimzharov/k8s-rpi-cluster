apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: transmission
  namespace: argocd
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          jellyfin: "installed"
  template:
    metadata:
      name: '{{name}}-transmission' 
    spec:
      project: k8s-rpi-apps
      sources:
      - repoURL: https://github.com/vadimzharov/k8s-rpi-cluster.git
        targetRevision: k8s-second
        path: k8s-rpi-apps-transmission
      - repoURL: https://github.com/vadimzharov/k8s-rpi-cluster.git
        targetRevision: k8s-second
        path: k8s-rpi-apps-ingresses/transmission-ingress
      destination:
        server: '{{server}}'
        namespace: transmission
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          